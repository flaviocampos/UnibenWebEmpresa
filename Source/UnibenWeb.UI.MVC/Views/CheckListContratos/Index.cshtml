@model IEnumerable<UnibenWeb.Application.ViewModels.CheckListContratoVM>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckItem)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CheckItem)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CheckListContratoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.CheckListContratoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CheckListContratoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
